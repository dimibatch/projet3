import { CampaignCodePipe } from './campaign-code.pipe';

describe('CampaignCodePipe', () => {
  it('create an instance', () => {
    const pipe = new CampaignCodePipe();
    expect(pipe).toBeTruthy();
  });
});
