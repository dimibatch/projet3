import { TestFamilyPipe } from './test-family.pipe';

describe('TestFamilyPipe', () => {
  it('create an instance', () => {
    const pipe = new TestFamilyPipe();
    expect(pipe).toBeTruthy();
  });
});
