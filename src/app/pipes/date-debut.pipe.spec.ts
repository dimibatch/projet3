import { DateDebutPipe } from './date-debut.pipe';

describe('DateDebutPipe', () => {
  it('create an instance', () => {
    const pipe = new DateDebutPipe();
    expect(pipe).toBeTruthy();
  });
});
