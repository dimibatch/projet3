import { DerogationPipe } from './derogation.pipe';

describe('DerogationPipe', () => {
  it('create an instance', () => {
    const pipe = new DerogationPipe();
    expect(pipe).toBeTruthy();
  });
});
