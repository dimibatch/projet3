import { RemoveTwinsPipe } from './remove-twins.pipe';

describe('RemoveTwinsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveTwinsPipe();
    expect(pipe).toBeTruthy();
  });
});
