import { PackModelPipe } from './pack-model.pipe';

describe('PackModelPipe', () => {
  it('create an instance', () => {
    const pipe = new PackModelPipe();
    expect(pipe).toBeTruthy();
  });
});
