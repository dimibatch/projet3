import { ColorMatPremPipe } from './color-mat-prem.pipe';

describe('ColorMatPremPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorMatPremPipe();
    expect(pipe).toBeTruthy();
  });
});
