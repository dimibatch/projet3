import { SelectAttributesPipe } from './select-attributes.pipe';

describe('SelectAttributesPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectAttributesPipe();
    expect(pipe).toBeTruthy();
  });
});
