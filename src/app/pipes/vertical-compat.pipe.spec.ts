import { VerticalCompatPipe } from './vertical-compat.pipe';

describe('VerticalCompatPipe', () => {
  it('create an instance', () => {
    const pipe = new VerticalCompatPipe();
    expect(pipe).toBeTruthy();
  });
});
