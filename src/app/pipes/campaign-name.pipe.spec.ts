import { CampaignNamePipe } from './campaign-name.pipe';

describe('CampaignNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CampaignNamePipe();
    expect(pipe).toBeTruthy();
  });
});
