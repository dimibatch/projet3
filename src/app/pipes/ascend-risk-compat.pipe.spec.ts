import { AscendRiskCompatPipe } from './ascend-risk-compat.pipe';

describe('AscendRiskCompatPipe', () => {
  it('create an instance', () => {
    const pipe = new AscendRiskCompatPipe();
    expect(pipe).toBeTruthy();
  });
});
