import { RealTypeAcPipe } from './real-type-ac.pipe';

describe('RealTypeAcPipe', () => {
  it('create an instance', () => {
    const pipe = new RealTypeAcPipe();
    expect(pipe).toBeTruthy();
  });
});
