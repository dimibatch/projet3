import { VracNumberPipe } from './vrac-number.pipe';

describe('VracNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VracNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
