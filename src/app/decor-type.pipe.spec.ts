import { DecorTypePipe } from './decor-type.pipe';

describe('DecorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DecorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
